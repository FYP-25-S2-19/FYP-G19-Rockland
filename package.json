{
  "name": "rockland-devtools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npx expo start --tunnel",
    "install-all": "npm install --prefix frontend",
    "setup-backend": "powershell -ExecutionPolicy Bypass -File setup_backend.ps1",
    "dev": "start cmd /k \"npm run setup-backend\" && npm run start"
  }
}
